<div class="piece AdminSetting">
<div class="pieceHeader">
<%= render 'piece_header' -%>
</div>
<div class="pieceBody">
<div class="pieceBodyHeader">
<%= render 'header' %>
<%= render 'search' %>
</div>
<div class="pieceBodyBody">
<div class="explain">表示対象となるメッセージは最大１行です。並び順が同じときは、更新日時の降順で表示します。</div>
<% if @items.blank? -%>
  <%= show_notice -%>
<% else -%>
  <table class="index">
    <tr>
      <th></th>
      <th>分類</th>
      <th>表示</th>
      <th>メッセージ</th>
      <th>表示順</th>
      <th>更新日</th>
    </tr>
    <% @items.each do | item| -%>
      <tr class="<%= cycle '', 'cycle' -%>">
        <td><%= link_to_show item %></td>
        <td><%= item.kind_show %></td>
        <td><%= item.state_show %></td>
        <td><%= safe{item.body.html_safe} -%></td>
        <td><%= item.sort_no -%></td>
        <td><%= safe{item.updated_at.strftime('%Y-%m-%d %H:%M')} %></td>
      </tr>
    <% end %>
  </table>
<% end -%>
</div>
<%= paginate(@items) %>
</div>
</div>