<div class="piece AdminMessage">
<%= render 'header_str' %>

<div class="pieceBody">
<%= render 'header' %>

<div class="explain">表示対象となるメッセージは最大２行です。　並び順が同じときは、更新日時の降順で表示します。</div>

<table class="index">
  <colgroup class="action"></colgroup>
  <colgroup class="mode"></colgroup>
  <colgroup class="state"></colgroup>
  <colgroup class="sort_no"></colgroup>
  <colgroup class="body"></colgroup>
  <colgroup class="updated_at"></colgroup>
  <tr>
    <th></th>
    <th>表示モード</th>
    <th>表示</th>
    <th>表示順</th>
    <th>メッセージ</th>
    <th>更新日</th>
  </tr>
  <% @items.each do |item| %>
  <tr class="<%= cycle('', 'cycle') %>">
    <td><%= link_to '詳細', {action: :show, id: item.id} %></td>
    <td><%= item.mode_label %></td>
    <td><%= item.state_label %></td>
    <td><%= item.sort_no %></td>
    <td><%=raw item.body %></td>
    <td><%=l item.updated_at if item.updated_at %></td>
  </tr>
  <% end %>
</table>

<%= paginate @items %>

</div>
</div><!--piece admin_messages-->
