<% if @show_flg %>
<% if @line_box.present? %>
<%= raw(hidden_field_tag('my_url', @schedule_move_url_directive.sub('%q', "#{@schedule_move_qs}"))) %>
<% end %>
<% if @line_box.present? %>
<%= raw(render(:partial => '/gw/admin/schedules/schedule_line_box', :locals=>{:d=>d, :mode=>mode, :move_mode=>:footer})) %>
<% end %>
<% end %>
