<%= render '/gw/admin/todos/header' %>
<div class="piece todo form">
<%= render '/gw/admin/todos/piece_header' %>
<div class="pieceBody"><div class="pieceBodyBody">

<%= form_for :item, :url => gw_todo_path(@item), :html => {:method => :put, :multipart => true, :id => 'itemForm'} do |f| %>
<%#= f.error_messages%>
<%= render 'form', f: f %>
<%= raw(submit_for_update(f, :caption=>'確定する')) %>
<% end %>
</div>
</div>
</div>
