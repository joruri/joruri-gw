<% if @items.length > 0 %>
<div id="message" class="<%= 'emergency' if @portal_mode.disaster_mode? %>">
  <% @items.each do |item| %>
    <div><%=sanitize item.body %></div>
  <% end %>
</div>
<% end %>
