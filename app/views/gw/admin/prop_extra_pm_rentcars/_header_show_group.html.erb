<% # app/views/gw/admin/prop_extra_pm_rentcar/_header.html.erb

link_s = %Q(<span class="current_menu">)
link_s_end = %Q(</span>)

lnks1 = (@hedder5lnk == 1 ? "#{link_s}明細（未集計含む）#{link_s_end}" : "<span>明細（未集計含む）</span>")
lnks2 = (@hedder5lnk == 2 ? "#{link_s}明細（集計済）#{link_s_end}" : "<span>明細（集計済）</span>")
lnks3 = (@hedder5lnk == 3 ? "#{link_s}明細（昨年度以前）#{link_s_end}" : "<span>明細（昨年度以前）</span>")

%>
<div class="menu">
<!-- <p>明細</p> -->
<%=link_to lnks1.html_safe, "#{@uri_base}/#{params[:id]}/show_group?mode=#{@s_order}"%>
<%=link_to lnks2.html_safe, "#{@uri_base}/#{params[:id]}/show_group?mode=#{@s_order}&total=1"%>
<%=link_to lnks3.html_safe, "#{@uri_base}/#{params[:id]}/show_group?mode=#{@s_order}&year=last"%>

</div>