<div class="piece AdminSetting">
<%= render 'header_str' %>
<div class="pieceBody">
<%= render 'gw/admin/portal_adds/menu' %>

<%= form_for @item, :as => :item, :url => {action: :update}, :html => {id: "itemForm", method: :put, multipart: true} do |f| %>
  <%= f.error_messages %>
  <%= render 'form', f: f %>
<% end %>

<div class="btReturn">
  <%= link_to '詳細に戻る', {action: :show} %>
</div>

</div>
</div>
