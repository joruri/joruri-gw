<%= required_head %>
<table class="show">
  <tr>
    <th>災害時に表示する掲示板<%= required('※') %></th>
    <td><%= f.collection_select :options, @item.bbs_options, :id, :title, {:include_blank => true} %></td>
  </tr>
</table>
