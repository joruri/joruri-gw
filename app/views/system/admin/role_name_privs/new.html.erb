<div class="piece AdminMessage">
<%= render 'header' %>
<div class="pieceBody">
<%= render 'system/admin/roles/header_menu' %>

<%= form_for :item, :url => "/system/role_name_privs#{@params_set}" do |f| %>
  <%= f.error_messages %>
  <%= render 'form', f: f %>
  <%= submit_for(f, {:caption => '保存'}) %>
<% end %>

</div>
</div>
