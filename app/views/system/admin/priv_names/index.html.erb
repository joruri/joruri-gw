<div class="piece AdminMessage">
<%= render :partial => 'header' %>

<div class="pieceBody">
<div class="editingBox">
  <span class="btNew"><%= link_to '新規作成', "/system/priv_names/new?#{@qs}" %></span>
  <span class="docLink"><%= link_to '管理者権限設定', "/system/roles"%></span>
</div>

<table class="index">
  <tbody>
    <tr>
      <th style="width: 60px;"></th>
      <th>権限対象名</th>
      <th>権限コード</th>
      <th style="width: 40px; text-align: center;">表示順</th>
      <th style="width: 60px; text-align: center;">ステータス</th>
    </tr>
    <% for item in @items %>
    <tr class="<%= cycle '', 'cycle' %>">
      <td style="text-align: center;"><%= link_to '詳細', "/system/priv_names/#{item.id}?#{@qs}" %></td>
      <td><%= item.display_name %></td>
      <td><%= item.priv_name %></td>
      <td style="text-align: center;"><%= item.sort_no %></td>
      <td style="text-align: center;"><%= item.state_label %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br/>
<%= paginate @items %>
<br/>

</div>
</div>
