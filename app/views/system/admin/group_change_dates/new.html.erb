<div class="piece AdminSetting AdminGroups">
<%= render 'header' %>
<div class="pieceBody">
<%= render 'navi' %>

<%= form_for :item, :url => {:action => :create} do |f| %>

<%= f.error_messages %>

<%= render 'form', f: f %>

<%= raw submit(:register) %>

<% end %>

</div>
</div>
