<div class="piece AdminSetting AdminGroups">
<%= render 'header' %>
<div class="pieceBody">
<%= render 'navi' %>

<%= form_for :item, :url => {:action => :update}, :html => {:method => :put} do |f| %>

<%= f.error_messages %>

<%= render 'form', f: f %>

<%= raw submit(:edit) %>

<% end %>
</div></div>
