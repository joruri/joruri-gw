<h4 style="margin-left:2em;">ＣＳＶ 出力</h4>
<div style="margin-left:3em;">
文字コードを選択して実行してください<br />
<span style="color:blue">出力対象を、指定の文字コードで出力します。<br />保存先は、ダウンロード時に指定できます。</span>
</div>
<br />
<%= form_for :item, url: "/gwbbs/csv_exports/#{@title.id}/export_csv", html: {method: :post, multipart: true} do |f| %>
  <table class="show">
    <tr>
      <th>文字コード</th>
      <td><%= f.radio_buttons :encoding, @item.encoding_options %></td>
    </tr>
  </table>
  <div style="text-align: center">
    <%= f.submit 'CSV出力' %>
  </div>
<% end %>
