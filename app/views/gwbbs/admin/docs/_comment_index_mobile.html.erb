<p class="bgAsh" >コメント</p>
<% @item.comments.each.with_index(1) do |item, i| %>
  <p class="leftPad2">
    <%= item.body %>
  </p>
  <p class="leftPad1">
  <%= item.creater_or_editor_label %>
  （<%= item.created_at.strftime('%Y-%m-%d %H:%M') %>）
  </p>
<% end %>
