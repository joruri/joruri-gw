<% brk_code = '' %>
<% @items.each do |item| %>
<% unless brk_code == item.latest_updated_at.strftime('%Y-%m-%d') %>
  <% brk_code = item.latest_updated_at.strftime('%Y-%m-%d') %>
  <p class="bgAsh"><%=item.latest_updated_at.strftime('%Y年%m月%d日') %></p>
<% end %>
<p class="leftPad1">
<%= link_to item.category.try(:name), "#{item.show_path}#{gwbbs_params_set}" %><br />
区分：<%= link_to item.inpfld_002, "#{item.show_path}#{gwbbs_params_set}" %><br />
文書名：<%= link_to item.title, "#{item.show_path}#{gwbbs_params_set}" %>
</p><br />
<% end %>
