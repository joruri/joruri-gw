<% brk_code = '' %>
<% @items.each do |item| %>
<% unless brk_code == item.category1_id.to_s %>
  <% brk_code = item.category1_id.to_s %>
  <p class="bgAsh"><%= item.category.try(:name) %></p>
<% end %>
<p class="leftPad1">
<%= link_to item.inpfld_006, "#{item.show_path}#{gwbbs_params_set}" %><br />
区分：<%= link_to item.inpfld_002, "#{item.show_path}#{gwbbs_params_set}" %><br />
文書名：<%= link_to item.title, "#{item.show_path}#{gwbbs_params_set}" %>
</p><br />
<% end %>