<% brk_code = '' %>
<% @items.each do |item| %>
<% unless brk_code == item.inpfld_006.to_s %>
  <% brk_code = item.inpfld_006.to_s %>
  <p class="bgAsh">通知日：<%= item.inpfld_006 %></p>
<% end %>
<p class="leftPad1">
<%= link_to gwbd_category_name(@d_categories, item.category1_id), "#{item.show_path}#{gwbbs_params_set}", :class=>title_read_flag(@title, item) %><br />
区分：<%= link_to item.inpfld_002, "#{item.show_path}#{gwbbs_params_set}" %><br />
文書名：<%= link_to item.title, "#{item.show_path}#{gwbbs_params_set}" %>
</p><br />
<% end %>
