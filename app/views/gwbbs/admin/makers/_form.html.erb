<% if @item.is_sysadm? || (@item.is_bbsadm? && (@item.create_section.blank? || @item.create_section_flag != '1')) %>
  <%= render 'form_admin', f: f %>
<% else %>
  <%= render 'form_section', f: f %>
<% end %>
