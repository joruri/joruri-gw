<%
_inherit_qs_s = ''
_inherit_qs_s = "&scn=#{URI.encode(params[:scn])}" unless params[:scn].blank?

t011 = %Q(<span>登録番号順</span>)
if @l3_current=='01'
  t011 = %Q(<span class="gwsub_current_menu">登録番号順</span>) if @t01_current == '11'
end
m011 = url_for(action: :admin_index) + "?sort=A#{_inherit_qs_s}"

t012 = %Q(<span>媒体名順</span>)
if @l3_current=='01'
  t012 = %Q(<span class="gwsub_current_menu">媒体名順</span>) if @t01_current == '12'
end
m012 = url_for(action: :admin_index) + "?sort=B#{_inherit_qs_s}"

t013 = %Q(<span>登録年月日順</span>)
if @l3_current=='01'
  t013 = %Q(<span class="gwsub_current_menu">登録年月日順</span>) if @t01_current == '13'
end
m013 = url_for(action: :admin_index) + "?sort=C#{_inherit_qs_s}"

t014 = %Q(<span>状況別</span>)
if @l3_current=='01'
  t014 = %Q(<span class="gwsub_current_menu">状況別</span>) if @t01_current == '14'
end
m014 = url_for(action: :admin_index) + "?sort=D#{_inherit_qs_s}"

t015 = %Q(<span>廃棄</span>)
if @l3_current=='01'
  t015 = %Q(<span class="gwsub_current_menu">廃棄</span>) if @t01_current == '15'
end
m015 = url_for(action: :admin_index) + "?sort=E&usage=s#{_inherit_qs_s}"

t016 = %Q(<span>CSV_OUT</span>)
if @l3_current=='01'
  t016 = %Q(<span class="gwsub_current_menu">CSV_OUT</span>) if @t01_current == '16'
end
m016 = url_for(action: :admin_index) + "?sort=F#{_inherit_qs_s}"

t03 = %Q(<span>CSV出力</span>)
if @l3_current=='03'
  t03 = %Q(<span class="gwsub_current_menu">CSV出力</span>)
end
m03 = '/gwsub/sb13/externalmedias/csvput'

t04 = %Q(<span>CSV登録</span>)
if @l3_current=='04'
  t04 = %Q(<span class="gwsub_current_menu">CSV登録</span>)
end
m04 = '/gwsub/sb13/externalmedias/csvup'

t06 = %Q(<span>外部媒体区分</span>)
m06 = '/gwsub/sb13/externalmediakinds/'

t02 = %Q(<span>戻る</span>)
m02 = url_for(action: :index)

%>
<div class="menu">
<%= link_to t015.html_safe, url_for(m015) -%>
<%= link_to t016.html_safe, url_for(m016) -%>
<%= link_to  t03.html_safe, url_for(m03) %>
<%= link_to  t04.html_safe, url_for(m04) %>
<%= link_to  t06.html_safe, url_for(m06) if !t06.blank? && @u_role==true -%>
<%= link_to  t02.html_safe, url_for(m02) %>
</div>
