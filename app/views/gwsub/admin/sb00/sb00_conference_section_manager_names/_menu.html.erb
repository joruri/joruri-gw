<%
t01 = %Q(<span>一覧</span>)
t02 = %Q(<span>登録</span>)
t03 = %Q(<span>同期処理</span>)
t04 = %Q(<span>CSV出力</span>)
t05 = %Q(<span>CSV登録</span>)
case @l1_current
when '01'
  t01 = %Q(<span class="gwsub_current_menu">一覧</span>)
when '02'
  t02 = %Q(<span class="gwsub_current_menu">登録</span>)
when '03'
  t03 = %Q(<span class="gwsub_current_menu">同期処理</span>)
when '04'
  t04 = %Q(<span class="gwsub_current_menu">CSV出力</span>)
when '05'
  t05 = %Q(<span class="gwsub_current_menu">CSV登録</span>)
else
end
-%>
<div class="menu">
<%= link_to t01.html_safe , @index_uri %>
<%= link_to t03.html_safe , url_for("#{@index_uri}synchro"),:confirm=>"所属情報との同期処理を行います。よろしいですか？" %>
<%= link_to t04.html_safe , url_for("#{@index_uri}csvput") %>

</div>
