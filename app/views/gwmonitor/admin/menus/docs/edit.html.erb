<% if @is_editable %>
  <%= render :partial => 'edit' %>
<% end unless @title.state == 'closed' %>

<% if @is_editable %>
  <%= render :partial => "show_result" %>
<% end if @title.state == 'closed' %>

<% unless @is_editable%>
  <%= render :partial => "show_result" %>
<% end if @title.spec_config == 5 %>
