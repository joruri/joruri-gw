<%
  #本来は一ヶ所で判別するのが良いが、変更の過渡期なのでわけてある。
  #ここだけ元レコードが記事では無くコントロールなので注意
  files_uri = "#{gwmonitor_base_attachments_path(item.id)}?title_id=#{item.id}"
%>
<table class="bbsFile">
  <tr>
    <th>添付ファイル</th>
    <td>
      <a href="#" onclick="chageDisp(this,'attach_form');return false;">閉じる▲</a>
    </td>
  </tr>
</table>

<table id="attach_form" border="0" class="show" style="display: ;">
  <tr>
    <td style="padding: 0px 0px;">
      <iframe src="<%= files_uri %>" style="width: 100%; height: 150px;" frameborder="0"></iframe>
    </td>
  </tr>
</table>